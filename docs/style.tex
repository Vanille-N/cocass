\usepackage{xcolor}
\usepackage{listings}
\usepackage[cache=false]{minted}

\usepackage{fontspec}
\setmonofont{JetBrains Mono}[
    Contextuals = Alternate,
    % Ligatures = TeX,
    Scale=0.7,
]

\usepackage{listings}
\lstset{
    basicstyle = \ttfamily,
    columns = flexible,
}
\makeatletter
\renewcommand*\verbatim@nolig@list{}
\makeatother

\definecolor{lightgray}{gray}{.95}
\setminted{
    bgcolor=lightgray,
    baselinestretch=0.70,
}

\setminted[c]{
}
\setminted[ocaml]{
}

\newcommand{\ml}[1]{\mintinline{ocaml}{#1}}
\newcommand{\cc}[1]{\mintinline{c}{#1}}

\AtBeginEnvironment{minted}{\vspace{-0.5em}\setlength{\parskip}{-0.8em}}


\let\olditemize=\itemize \let\endolditemize=\enditemize \renewenvironment{itemize}{\vspace{-0.7em}\olditemize \itemsep-0.45em}{\endolditemize}

\let\oldenumerate=\enumerate \let\endoldenumerate=\endenumerate \renewenvironment{enumerate}{\vspace{-0.7em}\oldenumerate \itemsep-0.45em}{\endoldenumerate}
